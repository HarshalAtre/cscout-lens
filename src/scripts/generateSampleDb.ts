/**
 * Sample Database Generator - DEPRECATED
 * 
 * NOTE: Per mentor guidance (dspinellis), the SQLite database approach has been removed.
 * Quote: "Dumping the whole project to SQLite sounds like an overkill and a waste of 
 * resources. I suggest to focus on how CScout can be directly queried for the required 
 * results."
 * 
 * CScout should expose its internal data structures through a well-designed REST interface.
 * The mock server (mockServer.ts) now uses in-memory data for development/testing.
 * 
 * See: https://github.com/dspinellis/cscout/issues/82
 * 
 * To run the mock server:
 *   npm run server
 * 
 * The mock server provides the same REST API endpoints as the real CScout server.
 */

console.log('NOTE: This script is deprecated.');
console.log('');
console.log('Per mentor guidance, the SQLite database approach has been removed.');
console.log('CScout should be queried directly via REST API.');
console.log('');
console.log('To start the mock server for development/testing:');
console.log('  npm run server');
console.log('');
console.log('The mock server uses in-memory data and provides the same');
console.log('REST API endpoints as the real CScout server.');
